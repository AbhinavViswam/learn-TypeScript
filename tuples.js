var user = ["wsfswvg", 1];
//also
user = [1, "dfdgf"]; // this is also possible , but there can be a situation where you will be needing the number to come first then the string , for such case...
var tUser = ["dfr", 2, 5]; //the order of types mentioned inside array can be understood using tuples
var rgb = [3, 3, 3];
var newUseR = [343, "fdf"];
newUseR.push("cfef");
